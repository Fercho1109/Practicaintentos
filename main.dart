import 'package:flutter/material.dart';
import 'package:registro_agua/paginas/home.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Home();
  }
}
